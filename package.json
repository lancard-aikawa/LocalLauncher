{
  "name": "local-launcher",
  "version": "1.0.0",
  "description": "Local web server launcher with TUI dashboard",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --compile --target=bun --outfile local-launcher.exe"
  }
}
